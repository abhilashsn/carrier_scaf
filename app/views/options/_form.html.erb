<%= form_for(@option) do |f| %>
  <% if @option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@option.errors.count, "error") %> prohibited this option from being saved:</h2>

      <ul>
      <% @option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answers %><br>
    <%= f.text_field :answers %>
  </div>
  <div class="field">
    <%= f.label :is_correct_answer %><br>
    <%= f.text_field :is_correct_answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
