<%= form_for(@questioner) do |f| %>
  <% if @questioner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@questioner.errors.count, "error") %> prohibited this questioner from being saved:</h2>

      <ul>
      <% @questioner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :is_active %><br>
    <%= f.check_box :is_active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
